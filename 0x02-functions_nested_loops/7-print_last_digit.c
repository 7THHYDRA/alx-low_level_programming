#include "main.h"
/**
 * print_last_digit - last digit.
 * @int: integer
 *
 * Return: return last digit.
 */

int print_last_digit(int)
{
	print_last_digit(20);

	_putchar(print_last_digit(20));
	return (print_last_digit(20));
}
