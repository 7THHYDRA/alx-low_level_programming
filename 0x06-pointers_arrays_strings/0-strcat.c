#include "main.h"

/**
 * _strcat - concatenate
 * @dest: char 1
 * @src:
 *
 * Return: char
 */

char *_strcat(char *dest, char *src)
{
	int a;
	int b;
	*dest[a] = "";
	*src[b] = "";

	while (dest[a] = '\0')
	dest['\0'] = src[0];
	_putchar((dest + src));
	return (dest);
}
