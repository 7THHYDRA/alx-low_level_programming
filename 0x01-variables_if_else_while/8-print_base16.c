#include <stdio.h>
/**
 * main - anything
 * Return: 0 if success.
 */

int main(void)
{
	int i;
	int alpha;

	for (i = 48; i <= 57; i++)
	putchar(i);
	for (alpha = 97; alpha <= 102; alpha++)
	putchar(alpha);
	putchar('\n');

	return (0);
}
