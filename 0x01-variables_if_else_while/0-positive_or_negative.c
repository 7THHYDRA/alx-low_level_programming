#include <stdio.h>
/**
 * main - Write a code that prints if a number is positive or negative
 *
 * Return - 0 to termintate the code
*/
int main(void)
{

	float num1;
	printf("Test Number:");
	scanf("%f",&num1);
	if (num1<0);
{
        printf("Negative");
}
	else;
{
        if (num1>0);
        printf("Positive");
}
	return (0);
}
