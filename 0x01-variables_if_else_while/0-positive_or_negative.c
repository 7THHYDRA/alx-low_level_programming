#include <stdlib.h>
#include <time.h>
/**
 * main - Anything
 *
 * Return : return 0 always if successful.
 */


int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	return (0);
}
